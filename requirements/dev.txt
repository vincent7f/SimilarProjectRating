# Similar Project Rating System - Development Dependencies
# 相似项目评分系统 - 开发依赖
#
# Install: pip install -r requirements/dev.txt
# Note: This implicitly requires base.txt dependencies.
# 注意：这隐式需要base.txt的依赖。

# Testing Framework / 测试框架
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12

# Code Formatting / 代码格式化
black>=24.0
isort>=5.13

# Code Linting / 代码检查
pylint>=3.0
flake8>=7.0

# Type Checking / 类型检查
mypy>=1.8

# Security Auditing / 安全审计
safety>=2.3
bandit>=1.7

# Import sorting / 导入排序
importlib-metadata>=7.0; python_version < "3.10"

# Pre-commit hooks / 预提交钩子
pre-commit>=3.6
