"""
Unit Tests - Module-level isolated tests.

Each test file focuses on a single module with mocked external dependencies.
Target coverage: >= 80% line coverage overall, >= 90% for core business logic.

单元测试 - 模块级隔离测试。
每个测试文件专注于单一模块，并mock外部依赖关系。
目标覆盖率：总体>=80%行覆盖率，核心业务逻辑>=90%。
"""
