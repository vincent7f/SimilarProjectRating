"""
Test Suite - Unit and integration tests.

Contains unit tests for individual modules and integration tests
for end-to-end workflows. Uses pytest framework with mock fixtures
for external dependencies (GitHub API, LLM services).

测试套件 - 单元测试和集成测试。
包含各个模块的单元测试以及端到端工作流的集成测试。
使用pytest框架，并为外部依赖（GitHub API、LLM服务）提供mock fixture。
"""
